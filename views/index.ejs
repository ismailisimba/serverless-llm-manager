<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gemma Management UI</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; padding: 20px; max-width: 800px; margin: auto;}
        textarea { width: 98%; margin-bottom: 10px; padding: 5px; font-size: 1rem; }
        button { padding: 10px 15px; font-size: 1rem; cursor: pointer; }
        label { display: block; margin-bottom: 5px; font-weight: bold;}
        .result-box { border: 1px solid #ccc; background-color: #f9f9f9; padding: 15px; margin-top: 20px; white-space: pre-wrap; /* Preserve whitespace and wrap lines */ font-family: monospace; }
        .error-box { border: 1px solid red; background-color: #ffeeee; color: #a00; padding: 15px; margin-top: 20px; white-space: pre-wrap; }
    </style>
</head>
<body>

    <h1>Gemma Prompt Interface</h1>

    <form action="/generate" method="POST">
        <div>
            <label for="prompt">Enter your prompt:</label>
            <textarea id="prompt" name="prompt" rows="8" required placeholder="e.g., Explain the theory of relativity in simple terms."></textarea>
        </div>
        <div>
            <button type="submit">Generate Response</button>
        </div>
    </form>

    <%# Conditionally display error messages %>
    <% if (locals.error) { %>
      <div class="error-box">
        <h2>Error:</h2>
        <pre><%= error %></pre> <%# Using <pre> to preserve error message formatting %>
      </div>
    <% } %>

    <%# Conditionally display the successful result %>
    <% if (locals.result) { %>
      <div class="result-box">
        <h2>Result:</h2>
        <pre><%= result %></pre> <%# Using <pre> to preserve result formatting (like code blocks, lists) %>
      </div>
    <% } %>

</body>
</html>